# Vercel 배포 설정 가이드

**작성일**: 2026-01-08
**목적**: Xperion Wiki Frontend를 Vercel에 배포하고 GitHub과 자동 연동 설정

---

## 🚀 Vercel 프로젝트 생성 및 GitHub 연동

### Step 1: Vercel에 로그인

1. **Vercel 웹사이트 접속**: https://vercel.com
2. **로그인** (GitHub 계정으로 로그인 권장)

### Step 2: 새 프로젝트 Import

1. **Dashboard로 이동**
2. **"Add New..." 버튼** 클릭 → **"Project"** 선택
3. **"Import Git Repository"** 섹션에서:
   - GitHub 저장소 목록이 보이지 않으면 → **"Adjust GitHub App Permissions"** 클릭
   - Vercel에 GitHub 저장소 접근 권한 부여

### Step 3: 저장소 선택

1. **`ginger1541/xperion-wiki`** 저장소 찾기
2. **"Import"** 버튼 클릭

### Step 4: 프로젝트 설정

#### 4.1 기본 설정

**Project Name**:
```
xperion-wiki
```

**Framework Preset**:
```
Vite
```
(자동으로 감지되어야 함)

**Root Directory**:
```
frontend
```
⚠️ **중요**: 반드시 `frontend`로 설정! (프로젝트 루트가 아님)

#### 4.2 Build & Output Settings

**Build Command**:
```
npm run build
```

**Output Directory**:
```
dist
```

**Install Command**:
```
npm install
```

이 설정들은 `frontend/vercel.json` 파일이 있으면 자동으로 적용됩니다.

#### 4.3 환경 변수 설정

**Environment Variables** 섹션에서 추가:

| Name | Value | Environment |
|------|-------|-------------|
| `VITE_API_URL` | `http://34.29.153.88` | Production, Preview, Development |

**실제 GCP 백엔드 IP로 변경하세요!**

**예시**:
```
Name: VITE_API_URL
Value: http://34.29.153.88
```

### Step 5: 배포 시작

1. 모든 설정 확인
2. **"Deploy"** 버튼 클릭
3. 빌드 로그 확인 (2-3분 소요)

---

## ✅ 배포 완료 후 확인

### 1. 배포 URL 확인

배포가 완료되면 다음과 같은 URL을 받습니다:

**Production URL**:
```
https://xperion-wiki.vercel.app
```
또는
```
https://xperion-wiki-[random-id].vercel.app
```

### 2. 배포 상태 확인

Vercel Dashboard → Project → Deployments에서:
- ✅ **Ready**: 배포 성공
- 🔨 **Building**: 빌드 중
- ❌ **Error**: 배포 실패 (로그 확인 필요)

### 3. 자동 배포 테스트

이제 GitHub에 푸시할 때마다 자동으로 배포됩니다:

```bash
# 간단한 변경 후 푸시
git add .
git commit -m "test: verify Vercel auto-deploy"
git push origin main
```

Vercel Dashboard에서 새 배포가 자동으로 시작되는지 확인하세요.

---

## 🔧 고급 설정

### 1. 커스텀 도메인 추가 (선택사항)

**Settings** → **Domains**에서:
1. 원하는 도메인 입력
2. DNS 설정 (Vercel이 안내)
3. SSL 자동 적용

### 2. 빌드 설정 최적화

**Settings** → **General** → **Build & Development Settings**:

**Node.js Version**:
```
18.x (권장)
```

**Framework**: Vite (자동 감지됨)

### 3. 환경별 배포 설정

**Production**: `main` 브랜치
**Preview**: 모든 Pull Request와 브랜치

**Settings** → **Git** → **Production Branch**:
```
main
```

---

## 🐛 트러블슈팅

### 문제 1: "Build Failed" 오류

**증상**: 배포가 실패하고 빌드 오류 발생

**해결**:
1. **빌드 로그 확인**: Deployments → 실패한 배포 → 로그 보기
2. **일반적인 원인**:
   - Root Directory가 `frontend`로 설정되지 않음
   - 환경 변수 (`VITE_API_URL`) 누락
   - `package.json`에 빌드 스크립트 없음

**로컬에서 테스트**:
```bash
cd frontend
npm install
npm run build
```

### 문제 2: "404 Page Not Found"

**증상**: 배포는 성공했지만 페이지 접속 시 404

**원인**: SPA 라우팅 문제

**해결**: `frontend/vercel.json` 파일 확인
```json
{
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "http://34.29.153.88/:path*"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

### 문제 3: API 호출 실패 (CORS 오류)

**증상**: Frontend는 로드되지만 API 호출 실패

**원인**: Backend CORS 설정 문제

**해결**: GCP 백엔드의 `.env` 파일 확인
```env
CORS_ORIGINS=https://xperion-wiki.vercel.app,https://xperion-wiki-*.vercel.app,http://localhost:5173
```

**백엔드 재시작**:
```bash
# GCP VM에서
cd /home/user/xperion-wiki/backend
sudo systemctl restart xperion-wiki
```

### 문제 4: 환경 변수가 적용되지 않음

**증상**: `VITE_API_URL`을 찾을 수 없음

**원인**:
- 환경 변수가 설정되지 않았거나
- `VITE_` 접두사 누락

**해결**:
1. **Settings** → **Environment Variables** 확인
2. 변수명이 정확히 `VITE_API_URL`인지 확인
3. 환경 변수 추가/수정 후 **Redeploy** 필요

**Redeploy 방법**:
- Deployments → 최근 배포 → **⋯** 메뉴 → **Redeploy**

### 문제 5: GitHub 저장소가 목록에 안 보임

**원인**: Vercel이 GitHub 저장소 접근 권한이 없음

**해결**:
1. **Add New** → **Project** 페이지에서
2. **"Adjust GitHub App Permissions"** 클릭
3. Vercel GitHub App 설정에서:
   - **Repository access** → "Only select repositories"
   - **`xperion-wiki`** 선택
4. **Save** 클릭

---

## 📋 배포 체크리스트

배포 전에 다음을 확인하세요:

- [ ] Vercel 계정 생성 및 GitHub 연동
- [ ] 프로젝트 Import (`ginger1541/xperion-wiki`)
- [ ] Root Directory = `frontend` 설정
- [ ] Framework = `Vite` 확인
- [ ] 환경 변수 `VITE_API_URL` 설정
- [ ] `frontend/vercel.json` 파일 존재
- [ ] 배포 성공 (빌드 로그 확인)
- [ ] Production URL 접속 테스트
- [ ] 자동 배포 테스트 (커밋 후 확인)
- [ ] 백엔드 CORS 설정 업데이트

---

## 🔄 자동 배포 플로우

```
┌─────────────────────────────────────────────────────────┐
│  Developer: git push origin main                        │
└─────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────┐
│  GitHub: Commit pushed to main branch                   │
└─────────────────────────────────────────────────────────┘
                            │
                            ├──────────────────────────────┐
                            ▼                              ▼
                   ┌─────────────────┐          ┌─────────────────┐
                   │  Backend 변경?  │          │ Frontend 변경?  │
                   └─────────────────┘          └─────────────────┘
                            │                              │
                       ✅ YES                         ✅ YES
                            │                              │
                            ▼                              ▼
                   ┌─────────────────┐          ┌─────────────────┐
                   │ GitHub Actions  │          │     Vercel      │
                   │     (CI/CD)     │          │  Auto-Deploy    │
                   └─────────────────┘          └─────────────────┘
                            │                              │
                            ▼                              ▼
                   ┌─────────────────┐          ┌─────────────────┐
                   │   GCP Server    │          │ Production URL  │
                   │ Backend Updated │          │ Frontend Updated│
                   └─────────────────┘          └─────────────────┘
```

---

## 📞 추가 도움

### Vercel 공식 문서
- **Vite 배포 가이드**: https://vercel.com/docs/frameworks/vite
- **환경 변수**: https://vercel.com/docs/environment-variables
- **커스텀 도메인**: https://vercel.com/docs/custom-domains

### 프로젝트 관련 문서
- [GCP 배포 가이드](./DEPLOYMENT_GCP.md)
- [CI/CD 설정 가이드](./CI_CD_SETUP.md)
- [진행 상황](./PROGRESS.md)

---

**작성자**: Development Team
**최종 업데이트**: 2026-01-08
